# Frontend

MVP frontend for:
- Onboarding Wizard
- Assignment Matrix

## Run
```bash
cd frontend
cp .env.example .env
npm install
npm run dev
```

Set ports/URLs through `.env`:
- `VITE_DEV_PORT`
- `VITE_API_BASE`
